"use client";import{useEffect,useState}from"react";import"./style.css";const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000";export default function Home(){const[d,setD]=useState({pending:[],recent:[]});const load=()=>fetch(API+"/api/dashboard").then(r=>r.json()).then(setD);useEffect(load,[]);const act=async(id,approve)=>{await fetch(API+"/api/videos/"+id+"/approval",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({approve})});load()};return <main><h1>YouTube Automation</h1><p>Founder control center</p><div className="card"><b>Pending approval: {d.pending.length}</b></div><h2>Needs your approval</h2>{d.pending.length===0?<p>Nothing waiting.</p>:d.pending.map(v=><div className="card" key={v.id}><h3>{v.title}</h3><p>{v.topic}</p><small>Policy {String(v.policy_passed)} · Rights {String(v.rights_cleared)} · Disclosure {String(v.disclosure_required)}</small><br/><button onClick={()=>act(v.id,true)}>Approve</button><button onClick={()=>act(v.id,false)}>Reject</button></div>)}<h2>Recent</h2>{d.recent.map(v=><div className="row" key={v.id}><span>{v.title}</span><span>{v.state}</span></div>)}</main>}
